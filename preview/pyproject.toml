[project]
name = "qkagent-html-preview"
version = "0.1.0"
description = "Local preview server for Quicker action intro HTML"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
]

[project.scripts]
qkagent-preview = "preview_server:main"
